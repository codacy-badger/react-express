const initialState = {
  title: 'Wellcome to React-Express !!',
  subtitle: 'React-Express is React.js and Express (SSR) Framework.',
}

export const indexReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}